{
  "name": "superslotstudio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node dist/tools/studio/cli.js cues --all --quiet",
    "check": "tsc -p tsconfig.json --noEmit",
    "demo": "npm run build && node scripts/serve-demo.mjs",
    "evidence:recovery": "tsc -p tsconfig.json && node dist/tools/evidence/recovery-cli.js",
    "studio": "npm run build && node dist/tools/studio/cli.js",
    "studio:fixtures": "npm run build",
    "dev:agent": "npm run build && npm run --prefix runtime/stake-studio-source dev:agent",
    "dev": "npm run build && npm run --prefix runtime/stake-studio-source dev",
    "mcp": "STAKE_STUDIO_AGENT=1 STAKE_STUDIO_URL=http://127.0.0.1:3001 node runtime/stake-studio-source/mcp/server.mjs",
    "test": "rm -rf dist && npm run build && node --test dist/test/*.test.js && node --test runtime/stake-studio-source/src/engines/presentation/*.test.js"
  },
  "devDependencies": {
    "@types/node": "20.19.9",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
